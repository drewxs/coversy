version: 2.1

orbs:
  node: circleci/node@5.0.0

jobs:
  build-and-test:  
    docker:
      - image: cimg/node:16.3
    steps:
      - checkout
      - node/install-packages

workflows:
  build-tests: 
    jobs:
      - build-and-test
  
  endpoint-tests:
    jobs:
      - node/test